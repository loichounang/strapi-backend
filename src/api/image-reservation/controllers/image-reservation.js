'use strict';

/**
 * image-reservation controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::image-reservation.image-reservation');
